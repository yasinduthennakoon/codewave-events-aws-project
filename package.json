{
  "name": "cloudwave-events-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Teaching example: serverless event management on AWS",
  "scripts": {
    "deploy:backend": "bash scripts/deploy-backend.sh",
    "remove:backend": "bash scripts/remove-backend.sh"
  },
  "engines": {
    "node": ">=20"
  }
}
